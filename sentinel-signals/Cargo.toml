[package]
name = "sentinel-signals"
version = "0.1.0"
edition = "2021"
description = "Passive signal detection for agent degradation — plugs into sentinel-core event bus"

[dependencies]
sentinel-types = { path = "../sentinel-types" }
